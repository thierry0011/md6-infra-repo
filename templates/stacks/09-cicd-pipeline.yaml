# ECR is IMMUTABLE: EventBridge fires on any push and pins the run to the pushed digest.
AWSTemplateFormatVersion: "2010-09-09"
Description: To-Do App - pipeline (ECR + S3 template source -> CodeBuild migrate -> CodeDeploy blue/green), started by EventBridge on ECR push.

Parameters:
  ProjectName:
    Type: String
    Default: md6-todo

  Environment:
    Type: String
    Default: dev
    AllowedValues: [dev, staging, prod]

  GitHubActionsRoleArn:
    Type: String
    Description: App repo CI role ARN - granted upload to source/* and KMS encrypt.

  DeploymentTimeoutMinutes:
    Type: Number
    Default: 15
    Description: Minutes before the old (blue) task set is terminated.

  AppKmsKeyArn:
    Type: String
    Description: Shared CMK ARN from the security stack.

  EcrRepositoryName:
    Type: String
    Description: ECR repository name (bootstrap stack).

  EcrRepositoryArn:
    Type: String
    Description: ECR repository ARN (bootstrap stack).

  AlbFullName:
    Type: String
    Description: ALB full name from the ecs-alb stack.

  BlueTargetGroupFullName:
    Type: String
    Description: Blue target group full name from the ecs-alb stack.

  GreenTargetGroupFullName:
    Type: String
    Description: Green target group full name from the ecs-alb stack.

  BlueTargetGroupName:
    Type: String
    Description: Blue target group name from the ecs-alb stack.

  GreenTargetGroupName:
    Type: String
    Description: Green target group name from the ecs-alb stack.

  ProdListenerArn:
    Type: String
    Description: Production listener ARN from the ecs-alb stack.

  EcsClusterName:
    Type: String
    Description: ECS cluster name from the ecs-alb stack.

  EcsServiceName:
    Type: String
    Description: ECS service name from the ecs-alb stack.

  TaskDefinitionFamily:
    Type: String
    Description: ECS task definition family from the ecs-alb stack (used by the migration step).

  TaskExecutionRoleArn:
    Type: String
    Description: ECS task execution role ARN from the ecs-alb stack.

  TaskRoleArn:
    Type: String
    Description: ECS task role ARN from the ecs-alb stack.

  AppSubnet1Id:
    Type: String
    Description: App subnet 1 id from the network stack (migration task network placement).

  AppSubnet2Id:
    Type: String
    Description: App subnet 2 id from the network stack.

  AppSecurityGroupId:
    Type: String
    Description: App security group id from the security stack (migration task network placement).

Resources:
  # Pipeline artifacts bucket; app CI uploads the deploy template here.
  PipelineArtifactBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Sub ${ProjectName}-${Environment}-pipeline-artifacts-${AWS::AccountId}
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        BlockPublicPolicy: true
        IgnorePublicAcls: true
        RestrictPublicBuckets: true
      BucketEncryption:
        ServerSideEncryptionConfiguration:
          - ServerSideEncryptionByDefault:
              SSEAlgorithm: aws:kms
              KMSMasterKeyID: !Ref AppKmsKeyArn
            BucketKeyEnabled: true
      VersioningConfiguration:
        Status: Enabled
      LifecycleConfiguration:
        Rules:
          - Id: ExpireOldArtifacts
            Status: Enabled
            ExpirationInDays: 30
            NoncurrentVersionExpiration:
              NoncurrentDays: 7

  # App CI may write only to source/*.
  PipelineArtifactBucketPolicy:
    Type: AWS::S3::BucketPolicy
    Properties:
      Bucket: !Ref PipelineArtifactBucket
      PolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Sid: AllowAppRepoCiUploadDeployArtifact
            Effect: Allow
            Principal:
              AWS: !Ref GitHubActionsRoleArn
            Action: s3:PutObject
            Resource: !Sub ${PipelineArtifactBucket.Arn}/source/*

  GitHubActionsArtifactUploadKmsPolicy:
    Type: AWS::IAM::Policy
    Properties:
      PolicyName: !Sub ${ProjectName}-${Environment}-github-actions-artifact-kms
      PolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Sid: EncryptDeployArtifact
            Effect: Allow
            Action:
              - kms:GenerateDataKey
              - kms:Decrypt
            Resource: !Ref AppKmsKeyArn
      Roles:
        - !Select [1, !Split ["/", !Ref GitHubActionsRoleArn]]

  # Runs manage.py migrate as a one-off Fargate task before deploy.
  MigrationProjectRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub ${ProjectName}-${Environment}-migration-codebuild
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: codebuild.amazonaws.com
            Action: sts:AssumeRole
      Policies:
        - PolicyName: RunMigrationTask
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Sid: RunAndDescribeTask
                Effect: Allow
                Action:
                  - ecs:RunTask
                  - ecs:DescribeTasks
                  - ecs:DescribeTaskDefinition
                  # Registers a migration task def revision with this build's image.
                  - ecs:RegisterTaskDefinition
                Resource: "*"
              - Sid: PassEcsRoles
                Effect: Allow
                Action: iam:PassRole
                Resource:
                  - !Ref TaskExecutionRoleArn
                  - !Ref TaskRoleArn
                Condition:
                  StringEquals:
                    iam:PassedToService: ecs-tasks.amazonaws.com
              - Sid: CodeBuildLogs
                Effect: Allow
                Action:
                  - logs:CreateLogGroup
                  - logs:CreateLogStream
                  - logs:PutLogEvents
                Resource: !Sub arn:${AWS::Partition}:logs:${AWS::Region}:${AWS::AccountId}:log-group:/aws/codebuild/${ProjectName}-${Environment}-migrate*
              - Sid: ArtifactBucketRead
                Effect: Allow
                Action:
                  - s3:GetObject
                  - s3:GetObjectVersion
                Resource: !Sub ${PipelineArtifactBucket.Arn}/*
              - Sid: KmsForArtifacts
                Effect: Allow
                Action: kms:Decrypt
                Resource: !Ref AppKmsKeyArn

  MigrationProject:
    Type: AWS::CodeBuild::Project
    Properties:
      Name: !Sub ${ProjectName}-${Environment}-migrate
      Description: Runs manage.py migrate before CodeDeploy shifts traffic.
      ServiceRole: !GetAtt MigrationProjectRole.Arn
      Artifacts:
        Type: CODEPIPELINE
      Source:
        Type: CODEPIPELINE
        BuildSpec: !Sub |
          version: 0.2
          phases:
            install:
              commands:
                - yum install -y jq >/dev/null
            build:
              commands:
                - IMAGE_URI=$(jq -r '.ImageURI' imageDetail.json)
                - echo "Running migrations against image $IMAGE_URI"
                # RunTask can't override the image, so register a revision that uses it.
                - >
                  aws ecs describe-task-definition --task-definition ${TaskDefinitionFamily}
                  --query taskDefinition > current-taskdef.json
                - >
                  jq --arg IMAGE_URI "$IMAGE_URI" '.containerDefinitions[0].image = $IMAGE_URI
                  | del(.taskDefinitionArn, .revision, .status, .requiresAttributes, .compatibilities, .registeredAt, .registeredBy)'
                  current-taskdef.json > migration-taskdef.json
                - >
                  NEW_TASK_DEF_ARN=$(aws ecs register-task-definition
                  --cli-input-json file://migration-taskdef.json
                  --query 'taskDefinition.taskDefinitionArn' --output text)
                - 'echo "Registered migration task definition: $NEW_TASK_DEF_ARN"'
                - >
                  TASK_ARN=$(aws ecs run-task
                  --cluster ${EcsClusterName}
                  --task-definition "$NEW_TASK_DEF_ARN"
                  --launch-type FARGATE
                  --network-configuration "awsvpcConfiguration={subnets=[${AppSubnet1Id},${AppSubnet2Id}],securityGroups=[${AppSecurityGroupId}],assignPublicIp=DISABLED}"
                  --overrides "{\"containerOverrides\":[{\"name\":\"app\",\"command\":[\"python\",\"manage.py\",\"migrate\",\"--noinput\"]}]}"
                  --query 'tasks[0].taskArn' --output text)
                - 'echo "Migration task: $TASK_ARN"'
                - aws ecs wait tasks-stopped --cluster ${EcsClusterName} --tasks "$TASK_ARN"
                - >
                  EXIT_CODE=$(aws ecs describe-tasks --cluster ${EcsClusterName} --tasks "$TASK_ARN"
                  --query 'tasks[0].containers[0].exitCode' --output text)
                - 'echo "Migration task exit code: $EXIT_CODE"'
                - test "$EXIT_CODE" = "0"
      Environment:
        Type: LINUX_CONTAINER
        ComputeType: BUILD_GENERAL1_SMALL
        Image: aws/codebuild/amazonlinux2-x86_64-standard:5.0
      TimeoutInMinutes: 15
      Tags:
        - Key: Name
          Value: !Sub ${ProjectName}-${Environment}-migrate

  # CodeDeploy blue/green for ECS.
  CodeDeployServiceRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub ${ProjectName}-${Environment}-codedeploy-ecs
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: codedeploy.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/AWSCodeDeployRoleForECS

  BlueTargetGroupUnhealthyAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub ${ProjectName}-${Environment}-blue-tg-unhealthy
      Namespace: AWS/ApplicationELB
      MetricName: UnHealthyHostCount
      Statistic: Maximum
      Period: 60
      EvaluationPeriods: 2
      Threshold: 0
      ComparisonOperator: GreaterThanThreshold
      TreatMissingData: notBreaching
      Dimensions:
        - Name: TargetGroup
          Value: !Ref BlueTargetGroupFullName
        - Name: LoadBalancer
          Value: !Ref AlbFullName

  GreenTargetGroupUnhealthyAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub ${ProjectName}-${Environment}-green-tg-unhealthy
      Namespace: AWS/ApplicationELB
      MetricName: UnHealthyHostCount
      Statistic: Maximum
      Period: 60
      EvaluationPeriods: 2
      Threshold: 0
      ComparisonOperator: GreaterThanThreshold
      TreatMissingData: notBreaching
      Dimensions:
        - Name: TargetGroup
          Value: !Ref GreenTargetGroupFullName
        - Name: LoadBalancer
          Value: !Ref AlbFullName

  CodeDeployApplication:
    Type: AWS::CodeDeploy::Application
    Properties:
      ApplicationName: !Sub ${ProjectName}-${Environment}-app
      ComputePlatform: ECS

  CodeDeployDeploymentGroup:
    Type: AWS::CodeDeploy::DeploymentGroup
    Properties:
      ApplicationName: !Ref CodeDeployApplication
      DeploymentGroupName: !Sub ${ProjectName}-${Environment}-dg
      ServiceRoleArn: !GetAtt CodeDeployServiceRole.Arn
      DeploymentConfigName: CodeDeployDefault.ECSAllAtOnce
      AutoRollbackConfiguration:
        Enabled: true
        Events:
          - DEPLOYMENT_FAILURE
          - DEPLOYMENT_STOP_ON_ALARM
      AlarmConfiguration:
        Enabled: true
        Alarms:
          - Name: !Ref BlueTargetGroupUnhealthyAlarm
          - Name: !Ref GreenTargetGroupUnhealthyAlarm
      BlueGreenDeploymentConfiguration:
        TerminateBlueInstancesOnDeploymentSuccess:
          Action: TERMINATE
          TerminationWaitTimeInMinutes: !Ref DeploymentTimeoutMinutes
        DeploymentReadyOption:
          ActionOnTimeout: CONTINUE_DEPLOYMENT
      DeploymentStyle:
        DeploymentType: BLUE_GREEN
        DeploymentOption: WITH_TRAFFIC_CONTROL
      ECSServices:
        - ClusterName: !Ref EcsClusterName
          ServiceName: !Ref EcsServiceName
      LoadBalancerInfo:
        TargetGroupPairInfoList:
          - ProdTrafficRoute:
              ListenerArns:
                - !Ref ProdListenerArn
            TargetGroups:
              - Name: !Ref BlueTargetGroupName
              - Name: !Ref GreenTargetGroupName

  # Pipeline: Source (S3 + ECR) -> Migrate -> Deploy.
  CodePipelineServiceRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub ${ProjectName}-${Environment}-codepipeline
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: codepipeline.amazonaws.com
            Action: sts:AssumeRole
      Policies:
        - PolicyName: PipelinePermissions
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Sid: ArtifactBucketAccess
                Effect: Allow
                Action:
                  - s3:GetObject
                  - s3:GetObjectVersion
                  - s3:PutObject
                  - s3:GetBucketVersioning
                Resource:
                  - !GetAtt PipelineArtifactBucket.Arn
                  - !Sub ${PipelineArtifactBucket.Arn}/*
              - Sid: KmsForArtifacts
                Effect: Allow
                Action:
                  - kms:Decrypt
                  - kms:GenerateDataKey
                Resource: !Ref AppKmsKeyArn
              - Sid: EcrSourceAction
                Effect: Allow
                Action:
                  - ecr:DescribeImages
                  - ecr:GetDownloadUrlForLayer
                  - ecr:BatchGetImage
                Resource: !Ref EcrRepositoryArn
              - Sid: EcrAuth
                Effect: Allow
                Action: ecr:GetAuthorizationToken
                Resource: "*"
              - Sid: RunMigrationBuild
                Effect: Allow
                Action:
                  - codebuild:StartBuild
                  - codebuild:BatchGetBuilds
                Resource: !GetAtt MigrationProject.Arn
              - Sid: CodeDeployDrive
                Effect: Allow
                Action:
                  - codedeploy:CreateDeployment
                  - codedeploy:GetApplication
                  - codedeploy:GetApplicationRevision
                  - codedeploy:GetDeployment
                  - codedeploy:GetDeploymentConfig
                  - codedeploy:RegisterApplicationRevision
                Resource: "*"
              - Sid: EcsAndPassRole
                Effect: Allow
                Action:
                  - ecs:RegisterTaskDefinition
                  - ecs:DescribeTaskDefinition
                Resource: "*"
              - Sid: PassEcsRoles
                Effect: Allow
                Action: iam:PassRole
                Resource:
                  - !Ref TaskExecutionRoleArn
                  - !Ref TaskRoleArn
                Condition:
                  StringEquals:
                    iam:PassedToService: ecs-tasks.amazonaws.com

  AppPipeline:
    Type: AWS::CodePipeline::Pipeline
    Properties:
      Name: !Sub ${ProjectName}-${Environment}-pipeline
      RoleArn: !GetAtt CodePipelineServiceRole.Arn
      ArtifactStore:
        Type: S3
        Location: !Ref PipelineArtifactBucket
        EncryptionKey:
          Type: KMS
          Id: !Ref AppKmsKeyArn
      Stages:
        - Name: Source
          Actions:
            # Deploy template uploaded by the app repo's CI.
            - Name: TaskDefTemplateSource
              ActionTypeId:
                Category: Source
                Owner: AWS
                Provider: S3
                Version: "1"
              Configuration:
                S3Bucket: !Ref PipelineArtifactBucket
                S3ObjectKey: source/appspec-taskdef.zip
                PollForSourceChanges: "false"
              OutputArtifacts:
                - Name: TemplateArtifactOutput
              RunOrder: 1
            - Name: ImageSource
              ActionTypeId:
                Category: Source
                Owner: AWS
                Provider: ECR
                Version: "1"
              Configuration:
                # Placeholder - EventBridge pins the real digest.
                ImageTag: latest
                RepositoryName: !Ref EcrRepositoryName
              OutputArtifacts:
                - Name: ImageSourceOutput
              RunOrder: 1
        - Name: Migrate
          Actions:
            - Name: RunDatabaseMigration
              ActionTypeId:
                Category: Build
                Owner: AWS
                Provider: CodeBuild
                Version: "1"
              InputArtifacts:
                - Name: ImageSourceOutput
              Configuration:
                ProjectName: !Ref MigrationProject
              RunOrder: 1
        - Name: Deploy
          Actions:
            - Name: DeployToECS
              ActionTypeId:
                Category: Deploy
                Owner: AWS
                Provider: CodeDeployToECS
                Version: "1"
              InputArtifacts:
                - Name: TemplateArtifactOutput
                - Name: ImageSourceOutput
              Configuration:
                ApplicationName: !Ref CodeDeployApplication
                DeploymentGroupName: !Ref CodeDeployDeploymentGroup
                TaskDefinitionTemplateArtifact: TemplateArtifactOutput
                TaskDefinitionTemplatePath: taskdef.json
                AppSpecTemplateArtifact: TemplateArtifactOutput
                AppSpecTemplatePath: appspec.yaml
                Image1ArtifactName: ImageSourceOutput
                Image1ContainerName: IMAGE1_NAME
              RunOrder: 1

  # Any successful ECR push starts the pipeline at that digest.
  EventBridgeInvokePipelineRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub ${ProjectName}-${Environment}-eventbridge-pipeline
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: events.amazonaws.com
            Action: sts:AssumeRole
      Policies:
        - PolicyName: StartPipeline
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action: codepipeline:StartPipelineExecution
                Resource: !Sub arn:${AWS::Partition}:codepipeline:${AWS::Region}:${AWS::AccountId}:${AppPipeline}

  EcrPushRule:
    Type: AWS::Events::Rule
    Properties:
      Name: !Sub ${ProjectName}-${Environment}-ecr-push-trigger
      Description: Starts the pipeline on any ECR push, pinned to the pushed digest.
      EventPattern:
        source: [aws.ecr]
        detail-type: ["ECR Image Action"]
        detail:
          action-type: [PUSH]
          result: [SUCCESS]
          repository-name:
            - !Ref EcrRepositoryName
      State: ENABLED
      Targets:
        - Id: StartCodePipeline
          Arn: !Sub arn:${AWS::Partition}:codepipeline:${AWS::Region}:${AWS::AccountId}:${AppPipeline}
          RoleArn: !GetAtt EventBridgeInvokePipelineRole.Arn
          InputTransformer:
            InputPathsMap:
              revisionValue: "$.detail.image-digest"
            InputTemplate: |
              {"sourceRevisions": [{"actionName": "ImageSource", "revisionType": "IMAGE_DIGEST", "revisionValue": "<revisionValue>"}]}

Outputs:
  PipelineArtifactBucketName:
    Value: !Ref PipelineArtifactBucket
    Description: App repo variable ARTIFACT_BUCKET.

  PipelineName:
    Value: !Ref AppPipeline

  CodeDeployApplicationName:
    Value: !Ref CodeDeployApplication

  CodeDeployDeploymentGroupName:
    Value: !Ref CodeDeployDeploymentGroup

  MigrationProjectName:
    Value: !Ref MigrationProject
